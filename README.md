cat > README.md << 'EOF'

# Airbnb CDMX — Modelación supervisada (Práctica 4)

Este repo contiene un notebook base para EDA + modelación supervisada usando datos públicos de **Inside Airbnb** (CDMX, snapshot 2025-09-27).

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
